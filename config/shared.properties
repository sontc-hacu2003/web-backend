# Database
spring.datasource.url=jdbc:postgresql://aws-1-ap-northeast-1.pooler.supabase.com:5432/postgres
spring.datasource.username=postgres.tywzrxamjanqqewfxgns
spring.datasource.password=Nhung300898@
spring.datasource.driver-class-name=org.postgresql.Driver

spring.jpa.open-in-view=false
backend.cors.allowed-origins=http://localhost:4200

logging.pattern.console=%d{yyyy-MM-dd HH:mm:ss.SSS} [%X{traceId}] [%X{userName}] %-5level - %msg%n
logging.pattern.file=%d{yyyy-MM-dd HH:mm:ss.SSS} [%X{traceId}] [%X{userName}] %-5level - %msg%n

# Security — MUST set JWT_SECRET env var in production (min 32 chars)
app.security.jwt.secret=${JWT_SECRET:dev-only-change-in-production}
app.security.jwt.expiration-seconds=3600
app.security.jwt.excluded-paths=/auth/**,/login,/register,/error

app.middleware.encryption.enabled=true
app.middleware.encryption.excluded-paths=/auth/public-key,/auth/signup,/auth/register,/login,/register,/error
